#include <iostream>
using namespace std;
#define int long long

int N, S;

main() {
    cin >> N;
    cin >> S;

    string NN, SS;
    NN = to_string(N);
    SS = to_string(S);
    for (int i = 0; i < 1000; i++) {
        string ii=to_string(i)
    }
    return 0;
}